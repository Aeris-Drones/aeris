# Simulation Recording

Simulation recording is configured through `software/sim/config/recording_profile.yaml`.

## Format

- Storage format: MCAP
- Default output location: `log/recordings`

## Example

```bash
python3 software/sim/tools/record_sim_session.py --config software/sim/config/recording_profile.yaml
```
