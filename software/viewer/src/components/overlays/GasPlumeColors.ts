import * as THREE from 'three';

// Color ramp: purple (low) -> green (medium) -> amber (high)
export const COLORS = {
  low: new THREE.Color(0x9333ea),
  medium: new THREE.Color(0x22c55e),
  high: new THREE.Color(0xf59e0b),
};
