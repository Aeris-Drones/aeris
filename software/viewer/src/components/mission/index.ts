/**
 * AERIS GCS Mission Components
 *
 * This module previously exported mission planning and execution components.
 * Current implementation uses a simplified mission model managed through
 * the fleet context and map components directly.
 *
 * TODO: Reintroduce mission-specific components when multi-mission
 * orchestration and complex flight pattern features are required.
 */
